version: '3'
services:
  app:
    build: .
    ports:
      - "8080"
    deploy:
      replicas: 3
      restart_policy:
        condition: on-failure
  nginx:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - app
